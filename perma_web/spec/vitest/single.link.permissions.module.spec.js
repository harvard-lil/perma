import { describe, expect, it } from 'vitest'
import * as SingleLinkModule from '../../static/js/single-link-permissions.module'

describe('Test single-link-with-permissions.module.js', () => {
  it('defines SingleLinkModule', () => {
    expect(SingleLinkModule).toBeDefined()
  })
})
