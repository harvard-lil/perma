LOAD DATABASE
    FROM mysql://root:password@mysqldb/perma
    INTO postgresql://perma:password@db/perma

    WITH data only, truncate,
        prefetch rows = 1000

   INCLUDING ONLY TABLE NAMES MATCHING ~/.*/
   EXCLUDING TABLE NAMES MATCHING 'django_migrations'
;
